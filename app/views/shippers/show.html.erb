<div class="page-header">
  <h3>Shipper: <%= @shipper.company_name %></h3>
</div>
<% if !notice.nil? %>
<p id="notice" class="alert alert-success"><%= notice %></p>
<% end %>
<div class="btn-group" style="margin-bottom: 20px">
  <a href="<%= edit_shipper_path(@shipper) %>" class="btn btn-small" title="Edit: <%= @shipper.company_name %>"><i class="icon-edit-sign"></i></a>
  <a href="<%= shippers_path %>" class="btn btn-small" title="Back to Lists"><i class="icon-reply"></i></a>
</div>
<div class="details">
  <div>
    <strong>COMPANY NAME:</strong>
    <span><%= @shipper.company_name %>&nbsp;</span>
  </div>
  <div>
    <strong>REFERENCE:</strong>
    <span><%= @shipper.reference %>&nbsp;</span>
  </div>
  <div>
    <strong>CREDIT TERMS:</strong>
    <span><%= @shipper.credit_term %> Days</span>
  </div>
  <div>
    <strong>ADDRESS:</strong>
    <pre><%= @shipper.full_address %>&nbsp;</pre>
  </div>
  <div class="details" style="margin-top: 10px">
    <% @shipper.custom_fields.where(field_key: "ADDRESS").each do |cf| %>
      <%= render 'shared/custom_field', :cf => cf %>
    <% end %>
    <% @shipper.custom_fields.where(field_key: "PHONE").each do |cf| %>
      <%= render 'shared/custom_field', :cf => cf %>
    <% end %>
    <% @shipper.custom_fields.where(field_key: "FAX").each do |cf| %>
      <%= render 'shared/custom_field', :cf => cf %>
    <% end %>
    <% @shipper.custom_fields.where(field_key: "EMAIL").each do |cf| %>
      <%= render 'shared/custom_field', :cf => cf %>
    <% end %>
    <% @shipper.custom_fields.where(field_key: "PIC").each do |cf| %>
      <%= render 'shared/custom_field', :cf => cf %>
    <% end %>
    <% @shipper.custom_fields.where(field_key: "URL").each do |cf| %>
      <%= render 'shared/custom_field', :cf => cf %>
    <% end %>
    <% @shipper.custom_fields.where(field_key: "CUSTOM").each do |cf| %>
      <%= render 'shared/custom_field', :cf => cf %>
    <% end %>
  </div>
  <div>
    <strong>NOTE :</strong>
    <pre><%= @shipper.notes %>&nbsp;</pre>
  </div>
</div>